let jutsus = require("./jutsus-db.json").jutsus;
console.log(jutsus);

// console.log(jutsus);

const url = document.URL;
console.log(url)
console.log('oi')

// window.addEventListener("load", (e) => {
//     document.querySelector("#name").textContent =
//         jutsus[Number(url.slice(-1)) - 1].name ||
//         "No jutsu found";
// });
